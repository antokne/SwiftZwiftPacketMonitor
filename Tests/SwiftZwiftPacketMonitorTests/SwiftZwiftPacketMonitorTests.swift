    import XCTest
    @testable import SwiftZwiftPacketMonitor

    final class SwiftZwiftPacketMonitorTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftZwiftPacketMonitor().text, "Hello, World!")
        }
    }
